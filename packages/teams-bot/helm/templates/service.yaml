apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: cip-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: teams-bot
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
