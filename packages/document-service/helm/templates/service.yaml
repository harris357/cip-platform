apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: cip-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: document-service
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
