# Slice 56: intent-classifier runtime deps.
#
# Pinned majors only — minor bumps OK, major bumps reviewed.
# scikit-learn version controls model artifact compatibility — DO NOT
# bump without re-training and re-eval.

fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.40
pydantic>=2.9,<3
scikit-learn>=1.5,<1.7
joblib>=1.4,<2
numpy>=1.26,<3
